import Link from 'next/link';
import { Home, Activity, MessageCircle, FileText } from 'lucide-react';
export function BottomNav({active}:{active:'home'|'vitals'|'chat'|'records'}){const cls=(k:string)=>`grid justify-items-center gap-1 text-xs ${active===k?'text-[#00D4FF]':'text-[#A0AEC0]'}`;return <nav className='fixed bottom-0 left-0 right-0 z-50 mx-auto flex max-w-md justify-around border-t border-cyan-400/20 bg-[#0A1628]/95 p-2 backdrop-blur'><Link href='/dashboard' className={cls('home')}><Home size={16}/>Home</Link><Link href='/dashboard' className={cls('vitals')}><Activity size={16}/>Vitals</Link><Link href='/chat' className={cls('chat')}><MessageCircle size={16}/>AI Chat</Link><Link href='/records' className={cls('records')}><FileText size={16}/>Records</Link></nav>}
